services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    networks:
      - reverseproxy
    labels:
      - "reversed.host=health.gottz.de"
      - "reversed.port=8080"

networks:
  reverseproxy:
    external: true
